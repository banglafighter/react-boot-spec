import BootstrapReactComponent, {BootstrapUIState} from "../common/spec-common-things";

export interface DialogFooterProps {
    children?: any
}


export default class DialogFooterSpec<P extends DialogFooterProps, S extends BootstrapUIState> extends BootstrapReactComponent<P, S> {}